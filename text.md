# クラウドPoC 計画見直し — 課題整理

作成: 2026-08-23
方法: 3エージェント(devops-sre / tech-lead / pm)による独立レビュー + 検証者による事実確認 + 自己レビュー2巡
対象: `クラウドPoC実施レポート.md` / `WordPress_Fargate化_移植差分.md` / `ローカルPoC実施レポート.md` / `iac/terraform`

> **この文書の位置づけ**
> 初版(同日)は「今回の検証項目に必要か」という費用対効果で評価していたが、
> **PoC本来のゴールを評価軸に据え直して全面的に書き直した**。
> 初版の「切る」判定の多くは、本来のゴールに照らすと誤りだった(→ 3章)。
>
> 【確認済】= 検証者が実物・実コマンドで確認 / 【要確認】= エージェント報告のみで未確認

---

## 0. PoC のゴール(評価軸)

現行はモノリス構成で、開発環境に単一障害点があり、本番更新はファイル差し替え。
これをモダンなアーキテクチャに変えることで、以下3点を達成できるかがPoCの課題。

| # | ゴール | 達成の判定基準 |
|---|---|---|
| **G1** | **開発難易度の低下** | **CI/CD自動化の可否。** main/productブランチへのマージで自動デプロイされること。エンジニアが毎回AWSコンソールにアクセスする必要がないこと。**既存アプリのダウンタイムを極力減らすこと** |
| **G2** | **AIによる自動化** | CI/CD・ソースコードレビュー・モニタリングチェックをAIが担える形になっていること |
| **G3** | **ヘッドレス化によるUIカスタマイズ性** | フロントがWPから独立し、UIを自由に変更できること |

**この3つが評価軸である。** 個々の検証項目は「このゴールに寄与するか」で判断する。

### 切る判定の原則

**必要性の有無で切らない。実施すること自体に意味がある。**
切ってよいのは **SICS・決済など、我々だけでは作業できない範囲のみ**(外部の資格情報・権限に依存するもの)。

---

## 1. 【最重要】G1(CI/CD自動化)の前提が2つ欠けている 【確認済】

ゴールG1に対して、**現状は出発点にすら立っていない**。今回のレビューで最も重い発見。

### 1-1. GitHub Actions のワークフローが1つも存在しない

```
find . -path '*/.github/workflows/*'  → 0件
```

`iac/terraform/github_oidc.tf` は OIDC プロバイダと `celes-poc-github-deploy` ロールを定義しており、**AWS側の受け口は用意されている**。ECR push + ECS デプロイ4アクションの権限も付いている。

しかし**「マージしたら動く」部分が実装されていない**。CI/CDの本体が無い。

→ G1 の判定基準そのものが未実装。**これを作ることが今回のPoCの中心作業の一つ**。

### 1-2. プロジェクトが git リポジトリですらない

```
git rev-parse --is-inside-work-tree  → gitリポジトリではない
ls -d .git                           → .git なし
```

main/product ブランチへのマージで自動デプロイ、という要件の**前提が存在しない**。

→ リポジトリ構成の設計(モノレポか分割か、どのディレクトリを何のブランチで動かすか)から決める必要がある。

**この2点は、WP Fargate化やデータ投入より前に着手すべき事項。**

---

## 2. 【重要】ダウンタイム最小化の要件と現行IaCが矛盾している 【確認済】

G1 は「既存アプリのダウンタイムを極力減らす」ことを含む。現行の `ecs.tf` はこれを満たさない。

### 2-1. デプロイ中に完全なダウンタイムが発生する設定になっている

```
desired_count = 1                              (variables.tf:40)
deployment_minimum_healthy_percent → 未指定 → AWS既定 50%
deployment_maximum_percent         → 未指定 → AWS既定 200%
```

**`desired_count = 1` に 50% を適用すると切り捨てで 0**。
つまりデプロイ時に**唯一のタスクを先に落としてから新タスクを起動する**動作になり、その間サービスが完全停止する。

現行の「ファイル差し替え」も瞬断があるが、**これでは改善したことにならない**。

### 2-2. 必要な修正

| 設定 | 現状 | あるべき値 | 理由 |
|---|---|---|---|
| `desired_count` | 1 | **2以上** | 1タスクではローリング更新が原理的に成立しない |
| `deployment_minimum_healthy_percent` | 未指定(50) | **100** | 更新中も既存タスクを維持 |
| `deployment_maximum_percent` | 未指定(200) | 200 | 新タスクを追加起動する余地 |
| `health_check_grace_period_seconds` | 未指定 | **60〜120** | 起動直後のヘルスチェック失敗による誤検知を防ぐ |

`deregistration_delay = 30`(alb.tf:21)は設定済みで妥当。
`deployment_circuit_breaker`(rollback=true)も設定済みで、**失敗時の自動ロールバックは実装されている**。

### 2-3. これはゴール達成の判定に直結する

「ダウンタイムを極力減らす」が要件である以上、**デプロイ中の実測ダウンタイムを測ることが検証項目になる**。
連続リクエストを流しながらデプロイし、5xx が何秒出るか(または0か)を記録する。

---

## 3. 初版の「切る」判定の撤回

初版では tech-lead の判定に従い、アーキ実証8項目中5項目を「切る」とした。
**ゴールに照らすと、これは誤りだった。** 切る原則は「SICS・決済など我々だけでは作業できない範囲のみ」。

| 項目 | 初版判定 | **改訂判定** | ゴールとの関係 |
|---|---|---|---|
| 2-1 IaC再現性 | 実質完了 | **実施** | **G1の中核。** 環境を壊して作り直せることが開発体験を変える。IAM検証で28リソースのapply→destroyは通ったが、WP込みでは未実証 |
| 2-2 SPOF解消 | 切る(AWSの仕様) | **実施** | **現行の最大リスクへの直接の回答。** 仕様として知っていることと、動いて見せることは別。クライアントに示す材料として必要 |
| 2-3 WAF | 残す | **実施** | 現行未導入の防御層 |
| 2-4 HTTPS+独自ドメイン | 保留 | **保留** | DNS発行主体が未確定(外部依存)。**切るのではなく保留** |
| 2-5 EFS共有 | 切る | **実施** | **G1に寄与。** 複数タスク化の前提であり、2-2(SPOF解消)と不可分。desired_count=2以上にする以上、必須 |
| 2-6 ElastiCache | 切る | **実施(ただし目的を訂正)** | **2-8の対策としては無効**(→4-1)。だが複数タスク化でのセッション共有の実証としては意味がある |
| 2-7 CI/CD | 切る | **最優先で実施** | **G1そのもの。** 現行「ファイル差し替え」の代替案。1章のとおり未実装 |
| 2-8 可観測性 | 切る | **最優先で実施** | **G2そのもの。** AIがモニタリングチェックを担う土台 |

**tech-lead の判定は技術的には正しかった**(Fargateの自己復旧はAWSの仕様、CI/CDは土台整備)。
しかし**PoCのゴールが「モダン化で何が変わるかを実証すること」である以上、動いて見せることに意味がある**。

### 再現確認15項目についても同様

初版は「12項目は結果が変わらないので切る」としたが、**実施する**。
ローカルとクラウドで同じ結果が出ることを確認するのが再現確認の目的であり、
**結果が変わらないことを、揃った環境で確認すること自体に意味がある**。

ただし tech-lead の技術的指摘は有効なので、**予測として記録**する(→ 14章)。

---

## 4. 技術的事実(初版から維持)

エージェントがコード・実物で確認した事実。**ゴールが変わっても事実は変わらない。**

### 4-1. 【最重要】2-8(ポイント割引¥0)は ElastiCache では解決しない 【確認済】

真因はセッションストアではなく**アイデンティティ伝搬**。

```php
// woocommerce-points-and-rewards/includes/class-wc-points-rewards-cart-checkout.php:547
$available_user_discount = WC_Points_Rewards_Manager::get_users_points_value( get_current_user_id() );
if ( $available_user_discount <= 0 ) { return 0; }
```

`get_users_points()` は `WHERE user_id = %d` で**DBを直接引き、セッションを参照しない**。
セッションが関与するのは部分利用の希望額のみで、上限を**下方修正するだけ**。ゼロを非ゼロに戻す経路が無い。

決定的な傍証: **ローカルは Redis が3週間稼働した状態で ¥0 を再現している**(`local-env-redis-1` が Up 3 weeks)。

原因は**既に特定済み**だった。`poc-front/app/api/store/[...path]/route.ts` のコメント:

> 注意: カートを操作するAPIをここに入れてはいけない。BFFがApplication Passwordで認証するとWPはそのユーザーのカートに切り替わり、ブラウザ(ゲストセッション)のカートと分離してしまう

→ **環境では解けない設計課題。** 2-6 ElastiCache は実施するが、**2-8の対策としてではない**。

### 4-2. 【G3に影響】ポイントプラグインに Store API 連携が皆無 【確認済】

適用ハンドラは `is_cart()` ゲート(`:571-573`)と `wp_ajax_*`(`:49`)にしか存在しない。
**認証を通しても Store API 経由ではハンドラが起動しない。** 設定では回避不可。

→ **ExtendSchema の自作が必須。** 移植項目として未計上。
→ この自作の成否は**G3(ヘッドレス化)の前提を左右する技術リスク**。ローカルで先行検証すべき。

### 4-3. 【G3に影響・既存提案文書に誤りあり】`Cart-Token` が存在しない 【確認済】

現行の WooCommerce は **6.3.1**。同梱 Blocks の `src/` を検索した結果:

```
grep -rn 'Cart-Token' celes-ec/.../woocommerce-blocks/src/  → 0件
```

**`Cart-Token` は存在しない。** セッション同定は `wp_woocommerce_session_*` cookie のみで、
`X-WC-Store-API-Nonce` は CSRF 用(認証・セッション同定の役割は持たない)。

**そして、既存の提案文書がこれを前提に書かれている。**

```
celes-spec/research/wip/proposal/headless-migration-proposal.md:86
  「カートをサーバーセッションではなく cart_token(Nonce ベースの JWT)で管理するため、
    ヘッドレスに親和性が高い」
celes-spec/research/wip/proposal/headless-migration-proposal.md:271
  「WooCommerce Store API はカートを cart_token(Nonce ベースの JWT)で識別する」
```

**この記述は現行バージョンでは成立しない。** Cart-Token は WooCommerce Blocks の後継バージョンで
導入された機能であり、6.3.1 には無い。

→ **影響**: ステートレスBFF設計の前提が崩れる。ヘッドレス化の「親和性が高い」という論拠の一つが、
現行バージョンでは使えない。**WooCommerce本体のアップグレードが前提**となり、
「バックエンド維持」方針と正面から衝突する。

→ **4-1(ポイント¥0)と同じ根で繋がっている。** Cart-Token があればBFFがカートを
ステートレスに同定でき、アイデンティティ伝搬の問題も別の解き方ができた可能性がある。
**現行バージョンではその道が塞がっている**ことを、設計決定(7章 Phase B-4)の前提にすべき。

### 4-4. ARM64 は問題なし。ローカルは既に arm64 だった 【確認済】

```
docker compose exec wordpress php -r 'php_uname("m")' → aarch64 / PHP 7.4.33
docker image inspect wordpress:6.1.1-php7.4-apache    → arm64/linux
```

**ローカルPoC 65件はすべて ARM64 上で通した結果。** x86_64 に妥協すると土俵が変わる。

**訂正**: `WordPress_Fargate化_移植差分.md` の65,66,70,159行は誤り(検証者が書いたもの)。
「ARM64ビルドが必要」「提供状況を要確認」「x86_64で妥協するのが現実的」はすべて撤回。

また「`php:7.4-apache` ベースで自前ビルド」も**推奨しない**。素の `php:` には gd/imagick/intl/bcmath/zip/exif/sodium が無く、`wordpress:` 派生には全部入っている。**現行イメージをそのまま使うのが正しい。**

### 4-5. 【ARM64の唯一の実ブロッカー】ewww-image-optimizer 【確認済】

92プラグイン+11テーマ中、同梱バイナリを持つのは**この1件のみ**。

```
file binaries/jpegtran-linux → ELF 32-bit LSB executable, Intel 80386
```

aarch64版が1本も無い。コード側(`unique.php:252-263`)は `php_uname('m')` を取得するだけで分岐に使わず、無条件に i386 を配置。**EWWWは有効化済み**。

**最大の罠**: ローカルではこのバイナリが**動いてしまう**(Docker Desktop の binfmt/Rosetta)。
**Graviton/Fargate の素のARM64ホストには無いので `Exec format error` になる。**

> 「Macで動いた」は根拠にならない。**Fargate上での実行を証跡に含めること。**

対処: apt で `libjpeg-turbo-progs optipng pngquant gifsicle webp` 導入 + `ewww_image_optimizer_skip_bundle`。
**注意**: EWWWを無効化するとローカルと差分が出るため、**環境差分を減らす方針なら apt 導入で揃える**。

追加PHP拡張は**ゼロ件**。`redis` 拡張すら不要(`object-cache.php:593-595` が Predis にフォールバック)。

### 4-6. データ量の実測 【確認済】

**DB は 9.5GB ではなく 15.04GB**(テキストダンプはインデックスを含まない)。

| テーブル | 計(GB) | 行数 |
|---|---:|---:|
| wp_woocommerce_order_itemmeta | 4.18 | 15,031,845 |
| wp_postmeta | 3.41 | 21,492,151 |
| wp_woocommerce_sessions | 1.97 | 736,566 |
| wp_itsec_logs | 0.97 | 496,934 |
| **合計(173テーブル)** | **15.04** | |

**セッション 736,566件は全件が期限切れ** 【確認済】

```sql
SELECT COUNT(*), SUM(session_expiry < UNIX_TIMESTAMP()) FROM wp_woocommerce_sessions;
→ 736566	736566
```

※ エージェント2体で報告が食い違ったため(736,566 / 541,484)、検証者が実DBで確認。**736,566が正**。

> **PoCを超えた指摘**: WooCommerceのセッションクリーンアップが機能していない。
> 本番RDSの肥大要因として**別途報告に値する**。

**uploads 3.7GB のうち 2.45GB は再生成可能なサムネイル**(50,185/55,337ファイル = 91%が生成物)。
EFSで支配的なのは容量ではなく**ファイル数**(メタデータ操作回数)。逐次 `cp` だと数時間かかる。

### 4-7. 踏み台EC2は権限が無い 【確認済】

`0_回答別紙_IAM権限設計.md:23-24`:
> EC2インスタンスは使用しないため、**EC2のインスタンス操作系権限は申請に含めません**

`DenyExpensiveInstanceTypes` は「Allowがある前提での上限Deny」であってAllowを与えない。
さらに `iam:PassRole` が両ポリシーとも `ecs-tasks.amazonaws.com` 条件付きで、**SSM用インスタンスプロファイルを渡せない**。

→ **ECS Exec で代替する**(`enable_execute_command = true` は設定済み、boundary に `EcsExecChannels` もある = 権限内)。

なお `RestoreDBInstanceFromDBSnapshot` のDeny懸念は、**STGはRDSではないのでスナップショット経路が最初から選択肢にない**。移植差分文書の当該記述は書き換えが要る。

### 4-8. RDS `db.t4g.micro` / gp3 20GB は不適

| 問題 | 内容 |
|---|---|
| 容量 | データだけで11〜15GB。**インポート中に一時領域で枯渇** |
| メモリ | 1GiB(バッファプール約0.75GiB)で11GB → ヒット率が壊滅。**「ヘッドレスが遅い」という誤った結論を招く** |
| CPU | クレジット枯渇でスロットリング。投入が数時間に化ける |

→ **db.t4g.medium(4GiB) / gp3 100GB / Single-AZ** を推奨。

**バージョンは 8.0.42 を明示指定**(`"8.0"` だと最新に流れる)。
理由: ダンプに `utf8mb3_general_ci` が17テーブル(**8.4で削除**)、`mysql_native_password` も8.4でビルトインから外れる。

**パラメータグループは移植差分文書の記述が過剰**。RDS mysql8.0 のデフォルトが compose のフラグと既に一致
(`default_authentication_plugin=mysql_native_password` / `sql_mode=NO_ENGINE_SUBSTITUTION`)。カスタムPGは文字セット目的のみ。

投入時のみ `innodb_flush_log_at_trx_commit=2` / `sync_binlog=0` にすると**2〜3倍速くなる**。

### 4-9. IaCに漏れているリソース

| 漏れ | 内容 |
|---|---|
| WP用ヘルスチェックパス | `/api/health` は Next.js 用。**WPには存在しない** |
| WP用SG | 既存 `aws_security_group.app` は**3000番のみ**許可。WPは80番 |
| `WP_HOME`/`WP_SITEURL` | `wp-config.php:24-25` で**ハードコード**。「コード変更不要」という記述と矛盾 |
| S3ステージング | 手元回線から毎回1.2GB+3.7GBは非現実的。**boundaryにS3読み取り権限が無い** |
| **GitHub Actions ワークフロー** | **1章のとおり不存在。G1の本体** |
| **git リポジトリ** | **1章のとおり不存在** |

---

## 5. WP Fargate 化は実施する

初版では3体の判定に従い「不要の可能性が高い」としたが、**撤回する**。

### 5-1. 実施する理由

| 理由 | 内容 |
|---|---|
| **① 環境差分の最小化** | 再現確認は「ローカルとクラウドで同じ結果か」を見るもの。環境が違えば**差分の原因が切り分けられない**。STG(Lightsail/x86_64/単一サーバー)では土俵が変わる。ローカルが arm64 と判明した以上、**Fargate Graviton なら CPU アーキまで一致する** |
| **② モダンアーキテクチャ自体が検証対象** | ゴールG1は「モノリス→モダン」の実証。STG流用では**現行構成のままで、検証したいことが検証できない** |
| **③ G1の判定基準を満たすため** | CI/CDでWPをデプロイできることがG1。STGへのファイル差し替えでは現行と同じ |

### 5-2. 3体が「不要」とした根拠と、その扱い

| 根拠 | 扱い |
|---|---|
| pm: 決済スコープ外化でSTG流用の推奨理由(webhook)が消滅 | **逆に読むべき。** webhookが論点でないなら、STGに縛られる理由も消えた |
| pm: WP必須なのはアーキ実証8項目中2項目 | **3章で全項目実施に改訂**したため前提が変わった |
| tech-lead: その2項目(EFS/ElastiCache)自体を切るべき | 同上 |
| devops-sre: 工数40〜80h | **PoCの本体コストとして計上する。** Phase 3の前倒しではない |

### 5-3. 位置づけの訂正

pm は「WP Fargate化はPhase 3の前倒し」としたが、**PoCのゴールがモダンアーキテクチャの実証である以上、これはPoC本体**。
40〜80h は「情報収集のための過剰投資」ではなく**検証の前提コスト**。

---

## 6. 判断が要る事項

### 判断1 データをどこまで揃えるか【要判断】

環境差分を減らす方針だが、データ量は工数とコストに直結する。

| 案 | 内容 | 環境差分 | 工数 |
|---|---|---|---|
| **A** | 全量15GB | ゼロ | 投入1〜1.5h/回 |
| **B(推奨)** | ログ・セッション系を除く**約11GB** | **実質ゼロ**(削るのは全件期限切れセッションとログのみ) | 投入1h/回 |
| C | 日付で絞る | **大** | — |

**C は採らない。** 「直近12ヶ月」で切ると全体で406件しか残らず、**testuser1の263件が消えて2-9が検証不能**になる(件数ではなく「そのユーザーの関連行」が要る)。参照整合を壊すと「環境起因の不合格」が出て、**再現確認の目的そのものが崩れる**。

**B で削る対象**(すべて 2-1〜2-15 の依存外を確認済み):
```
wp_woocommerce_sessions  1.97GB  ← 全件期限切れ
wp_itsec_logs            0.97GB
wp_itsec_temp / wp_wpml_mails / wp_irrp_redirection_logs / wp_spfwc_log
wp_actionscheduler_logs / wp_afwc_hits / wp_ewwwio_images
wp_wc_points_rewards_user_points_log  ← 履歴ログであり残高ではない
```
**`wp_comments` は残す**(2-15 レビューが依存)。

### 判断2 uploads をどこまで載せるか【要判断】

| 案 | 内容 | 環境差分 |
|---|---|---|
| A | 全量3.7GB/55,337ファイル | ゼロ。ただし**投入に数時間** |
| **B(推奨)** | オリジナル1.25GB/5,152ファイルのみ | サムネイル404が出るが**JSONレスポンスは同じ** |
| C | 載せない(`upload_url_path` を本番に向ける) | ローカルPoC当初と同じ方式 |

**判断材料**: 2-1〜2-15 は Store API / 独自REST の**JSONレスポンス検証**であり、画像バイナリ配信の検証ではない。
ただし環境差分を減らす方針なら A も選択肢。**投入時間とのトレードオフ**。

### 判断3 RDS/EFS を destroy サイクルに含めるか【要判断】

- 含める → 毎回2時間の再投入
- 含めない → 月約1,900円が常時発生、再構築15分

**「作って壊す」で壊すべきは時間課金の大きい ALB/Fargate のみ**というのが devops-sre の見解。

### 判断4 リポジトリ構成【要判断・G1の前提】

git リポジトリが無いため、以下を決める必要がある。

- モノレポか、`poc-front` / `iac` / WP を分けるか
- main / product ブランチの運用(どちらが何にデプロイされるか)
- **既存の本番コード `celes-ec`(読み取り専用)をどう扱うか**

### 判断5 S3読み取り権限の追加申請【要判断・外部依存】

データ投入をS3経由にするなら、`celes-poc-boundary` にS3系Allowの追加申請が必要。
現状のポリシーには無い。**申請のリードタイムを考えると早めに出すべき。**

---

## 7. 実施順序

### Phase A: G1の前提を作る(環境不要・最優先)

1. **git リポジトリの初期化とブランチ戦略の決定**(判断4)
2. **GitHub Actions ワークフローの作成** — main/product マージで ECR push → ECS デプロイ
3. `ecs.tf` のダウンタイム対策(2-2の表のとおり修正)

**この3つは AWS 環境が無くても着手できる。** そしてG1の中核。

### Phase B: 環境不要の設計決定(並行可)

4. **セッション/認証伝搬の設計決定** — 4-1のとおり**クラウドがあっても解けない**。紙とローカルで決着する
5. **ポイント Store API 拡張(ExtendSchema)のプロトタイプ** — 4-2により**自作の成否がG3の前提**。ローカルで検証可能
6. **セキュリティ是正2件**(XSS・星範囲) — ローカル可

### Phase C: クラウド環境構築

7. S3ステージング(判断5の権限が前提)
8. RDS + EFS 作成 → データ投入(判断1・2)
9. WP用イメージビルド(**EWWW の arm64 対処を含む** → 4-5)
10. WP ECS + ALBルーティング
11. **CI/CD の実地確認** — Phase A で作ったワークフローが実際に動くか

### Phase D: 検証実施

12. アーキ実証8項目(3章の改訂判定)
13. 再現確認15項目 + ローカル実測値との対比
14. **性能ベースライン取得**(→14章)

### 外部依存で保留

- 2-4 HTTPS+独自ドメイン(DNS発行主体が未確定)
- 決済11TC / SICS 8TC / 計測5TC(**我々だけでは作業できない範囲**)
- 4章 定期購入8TC(Subscriptionsライセンスが別ブロッカー)

---

## 8. G2(AIによる自動化)の検証設計

**初版にはこの観点が完全に欠落していた。** ゴールG2は以下3点をAIが担えること。

| 対象 | 検証内容 | 前提 |
|---|---|---|
| **CI/CD** | AIがワークフローの失敗を検知し、原因を特定できるか | Actionsのログが構造化されていること |
| **ソースコードレビュー** | AIがPRに対してレビューを行えるか | **gitリポジトリとPRベースの開発フロー**(判断4) |
| **モニタリングチェック** | AIがCloudWatchのメトリクス・ログから異常を判断できるか | **構造化ログ**。`ecs.tf` は awslogs 設定済みだが、**アプリ側のログ形式は未確認** |

**判定基準の案**: 「AIが判断できる形」を測るには、実際にAIに判断させるのが最も確実。
- 疑似障害(タスク停止・5xx発生)を起こし、**AIがログとメトリクスだけで原因を特定できるか**を試す
- 意図的にバグを含むPRを作り、**AIがレビューで検出できるか**を試す

これは既存の `クラウドPoC実施レポート.md` 2-8「可観測性」を**G2の判定基準に拡張する**もの。

---

## 9. コストの再算定

### 9-1. 3文書で10倍の開きがある

| 文書 | 月額 | 前提 |
|---|---:|---|
| PoCクラウド構成_提案.md(**クライアント提出済み**) | **約1,250円** | 月4日稼働・WPはSTG流用 |
| evidence/No.IAM_権限検証 | 約6,500円 | 実測(常時稼働換算) |
| WordPress_Fargate化_移植差分.md | 約12,700円 | 上記+WP+EFS+ElastiCache |

### 9-2. 月4日(96時間)稼働での再算定

時間課金は 96/730 = **約13%**。ただし **G1のダウンタイム対策で `desired_count=2` にするため、Fargate費は倍**になる。

| 項目 | 常時(月) | 96h稼働 | 備考 |
|---|---:|---:|---|
| ALB | 約3,500円 | 約460円 | |
| フロント Fargate ×2 | 約4,000円 | 約520円 | **desired_count=2** |
| WP Fargate ×2 | 約8,000円 | 約1,060円 | **desired_count=2** |
| RDS db.t4g.medium | 約8,000円 | 約1,050円 | |
| **RDS gp3 100GB** | 約1,800円 | **約1,800円** ★ | |
| **EFS** | 約70円 | 約70円 ★ | |
| ElastiCache cache.t4g.micro | 約2,000円 | 約260円 | **実施に改訂** |
| その他(ECR/Secrets/Logs/転送) | — | 約300円 | |
| **合計** | 約27,400円 | **約5,500円** | |

★ = 稼働時間に依らない費目

### 9-3. `budget_limit_usd = 15` は未修正 【確認済】

`iac/terraform/variables.tf:82` が default=15 のまま。月4日稼働で約37USDなので不足。
**40〜50 USD が妥当。** ただし低めに置くと **destroy忘れの検知装置**として機能する。
`alert_email` が空だと通知系が作られない点も要設定。

### 9-4. クライアント説明が必要

提出済み1,250円 → 5,500円(96h稼働)。**技術判断ではなく対外的な説明責任の問題。**
ただし「WP Fargate化はPoCのゴール達成に必要」という説明が立つため、根拠は明確。

---

## 10. 文書・計画の整合性

### 10-1. 番号が衝突している

| 番号 | ローカルPoCレポート | クラウドPoCレポート |
|---|---|---|
| **2-8** | ポイント割引¥0(最大の設計課題) | 可観測性 |

「2-1〜2-15の再現確認」という言い方をすると**4つ目のID体系**が生まれる。
既存の `No.X` 体系(evidence/ が既にこれで切られている)に揃えるべき。

### 10-2. evidence の上書きリスク

ローカルとクラウドで同じ `No.34_BFF疎通` を検証すると**既存を上書きする**。
→ `evidence/cloud/No.X/` のような階層分けが必須。

ローカル15項目に対し evidence は8ディレクトリのみ。**7項目は独立した証跡がない**(ログのみ)。

### 10-3. 依頼台帳が実態と乖離している

`シュウエイ依頼タスクリスト.csv`(最終更新 07-27)は4件すべて「未依頼」だが:

| No. | 内容 | CSV | 実態 |
|---|---|---|---|
| 1 | WP管理画面アカウント | 未依頼 | Q8で**回答受領済み**(08-04) |
| 2 | STG DBダンプ | 未依頼 | **07-31に復元完了** |
| 3 | STG SSH | 未依頼 | No.2完了により不要 |
| 4 | 本番DB接続ユーザー | 未依頼 | 唯一、本当に未依頼の可能性 |

**約4週間更新されていない。** これを根拠に判断すると誤る(検証者も初版で誤った)。

クラウドPoCレポート末尾の「決済サンドボックス・SICS権限はクライアント依頼(4件)に依存」は**事実誤認**。CSVの4件にどちらも入っていない。

### 10-4. 進捗CSVが古い

`PoC全体進捗_俯瞰.csv`(最終更新 08-08)は「ポイント三重併用: 未」だが、ローカルPoC 2-8 で**実施済み(一部合格)**。

### 10-5. docs-viewer に3週間分が未反映

`docs-viewer/index.html`(07-29) / `proposal.html`(07-30) に
**「ローカルPoC」「クラウドPoC」の文字列が0件**。

08-04以降の成果(117件のテスト結果、ポイント割引¥0、受注ステータス自動遷移が移植不要)が
**クライアント向けビューアに一切載っていない**。クラウドPoCとは独立して優先度が高い。

---

## 11. 今すぐ出すべき依頼(リードタイム逆算)

決済・SICSが別途でも、**依頼だけは今出すべき**。
PoC計画書349行目に「サンドボックス資格情報の入手リードタイムが長いため、依頼だけ先行して出す」と自ら記載がある。

1. 決済4社のサンドボックス資格情報(Sony e-Scott / Amazon Pay / Paidy / PayPay)+ **webhook登録URLの差し替え権限の所在**
2. SICS管理画面(admin.sd-lines.com)の閲覧・設定権限
3. **SICS継続可否(Q12)の督促** — 08-04から19日待ち。**最優先の律速**
4. WooCommerce Subscriptions のライセンス所在
5. Route53レコードの発行主体(`PoC開発用AWS構成_概算.md:161`「シュウエイ様アカウント」)
6. **S3読み取り権限の boundary への追加**(判断5)

**あわせて `シュウエイ依頼タスクリスト.csv` を「依頼日」列付きで作り直す**(現状は待機日数が追えない)。

---

## 12. スコープ外だが重要なリスク

### 12-1. nonce無効化フィルタの存在 — **WooCommerce自身が使っている** 【確認済】

`AbstractCartRoute.php:172` に **`woocommerce_store_api_disable_nonce_check`** が実在。
trueにすると Store API の CSRF保護が**全面無効化**される。

```php
// packages/woocommerce-blocks/src/StoreApi/Routes/AbstractCartRoute.php:172
if ( apply_filters( 'woocommerce_store_api_disable_nonce_check', false ) ) {
```

**当初の想定より危険な事実**: WooCommerce 本体が**自分でこのフィルタを使っている**。

```php
// packages/woocommerce-blocks/src/BlockTypes/Checkout.php:340
add_filter( 'woocommerce_store_api_disable_nonce_check', '__return_true' );
// :345
remove_filter( 'woocommerce_store_api_disable_nonce_check', '__return_true' );
```

Checkoutブロックのレンダリング中に一時的に有効化し、直後に外している。
**つまり「公式に使われている手法」という体裁があり、模倣される正当性が生まれてしまう。**

> ヘッドレス化の実装中に「nonceが邪魔だから」と有効化される誘惑が非常に強い。
> しかも**本体が使っているという前例がある**ため、レビューで止めにくい。
> **現行の「AJAX9種すべてnonce検証なし」と同じ穴を新コードで再現することになる。**
>
> **対策**: `add_filter` の使用禁止を実装ルールとして明文化し、
> **CI で grep 検出して落とす**(G1のCI/CD自動化と、G2のAIコードレビューの具体的な適用先になる)。

### 12-2. `get_current_user_id()` 依存の横展開

4-1 は氷山の一角。**会員価格・定期購入・ギフトカードなど、会員コンテキスト依存の全機能で再発する。**
棚卸しが必要で、見積の「独自REST実装」項目の根拠に直接効く。

### 12-3. WooCommerceセッションの肥大(現行の運用課題)

736,566件が全件期限切れ。クリーンアップが機能していない。**本番RDSの肥大要因として別途報告。**

---

## 13. 訂正が必要な既存文書

| ファイル | 箇所 | 内容 |
|---|---|---|
| `WordPress_Fargate化_移植差分.md` | 65,66,70,159行 | ARM64の記述。**検証者が書いた誤り** |
| 同上 | ③自前ビルド推奨 | `wordpress:` 派生をそのまま使うべき |
| 同上 | ⑤ElastiCache | 2-8の対策としては無効(実施はする) |
| 同上 | DB 9.5GB | 実体は15.04GB |
| 同上 | ④「コード変更不要」 | `WP_HOME`/`WP_SITEURL` はハードコード |
| 同上 | `RestoreDBInstanceFromDBSnapshot` | STGはRDSでないので選択肢に無い |
| `クラウドPoC実施レポート.md` | 85行 | ElastiCacheが2-8の検証環境の前提 → **技術的に誤り** |
| 同上 | 1-1 目的定義 | ゴール3点(G1/G2/G3)に書き換え |
| 同上 | 末尾 | 「依頼4件に依存」は事実誤認 |
| `ローカルPoC実施レポート.md` | 2-8 | 真因は正しいが「カートとゲストのカートが分離」が**ElastiCacheという誤処方を連想させる** |
| `iac/terraform/variables.tf` | 40行 | `desired_count = 1` → **2以上**(G1のダウンタイム要件) |
| 同上 | 82行 | `budget_limit_usd = 15` → 40〜50 |
| `iac/terraform/ecs.tf` | サービス定義 | **deployment_minimum_healthy_percent 等が未指定**(2-2) |
| **`celes-spec/.../headless-migration-proposal.md`** | **86, 271行** | **`cart_token` を前提に「ヘッドレスに親和性が高い」と記載。WooCommerce 6.3.1 には存在しない**(→4-3)。提案の論拠に関わるため優先度が高い |

---

## 14. 検証を「情報収集」として成立させるために

再現確認は「同じ結果が出た」だけでは情報価値が薄い。**差分が出たときに意味がある。**

### 14-1. 必要な成果物

1. **ローカル実測値との対比列** — 現行TC表は「結果/実測メモ」の2列で**ローカル値の列が無い**。「ローカル¥3,500 → クラウド¥3,500」と並べないと再現確認にならない
2. **性能ベースラインの取得** — PoC計画書に「⓪性能ベースライン(+4h)」があり、ローカルでは未取得。クラウドで初めて「ALB→Fargate→WP→RDS」の実レイテンシが測れる。**同じ作業で工数見積の精度が上がる**
3. **デプロイ中のダウンタイム実測**(→2-3) — G1の判定基準
4. **未取得のまま残る項目の明示**

### 14-2. クラウドで結果が変わりうる項目(事前予測)

tech-lead の技術的分析に基づく。**予測を立ててから実施することで、差分の意味が読める。**

| 項目 | 予測 | 根拠 |
|---|---|---|
| 2-8 ポイント割引 | **変わらない(¥0のまま)** | 4-1。ElastiCache導入でも同じ |
| 2-15 レビュー不正入力 | **変わらない(不合格のまま)** | `kses.php:2198` のコード仕様。実行基盤と無関係 |
| 2-10 受注ステータス | **変わらない** | WP内部で完結 |
| 2-1〜2-7, 2-9, 2-11, 2-12, 2-14 | **変わらない** | 同一プロセス内のPHP実行結果 |
| **2-2 商品詳細の画像URL** | **変わる可能性** | Store APIの画像URLはsiteurl基準で動的生成。**ドメイン変更で壊れうる** |
| **2-13 認証** | **WAFにより変わる可能性** | ログイン試行のレート制限・列挙攻撃のブロック |
| **2-4 カート投入 / 2-7 GC** | **desired_count=2 で変わる可能性** | セッションはDB(`wp_woocommerce_sessions`)なので理論上は維持されるが、**複数タスクでの実証は未実施** |

**予測が外れた項目こそが、最も価値のある情報。**

---

## 付録: 自己レビュー記録

本文書は作成後、2巡の自己レビューを実施した。

**1巡目で修正した点**
- 3章の「切る」判定について、初版の判定を機械的に反転させていた箇所を、ゴールとの対応で書き直し
- 9-2 のコスト表で `desired_count=2` への変更をFargate費に反映(初版は1タスク前提のまま)
- 2章のダウンタイム問題を、初版では「設定漏れ」程度に扱っていたのを、**G1の要件と矛盾する**という位置づけに格上げ
- 14-2 の予測表に 2-4/2-7 を追加(複数タスク化により未検証領域が生まれるため)

**2巡目で修正した点**
- 1章と7章 Phase A の重複を整理。1章は「事実」、7章は「順序」に役割を分離
- 判断2(uploads)で「B推奨」としつつ本文が全量前提で書かれていた矛盾を解消
- 4-5 EWWW の対処方針を「無効化」から「apt導入で揃える」に変更(**環境差分を減らす方針との整合**)
- 6章の判断1で C案を「採らない」と明示(初版は3案併記で判断を委ねていたが、testuser1のデータが消える具体的な不整合があるため、推奨を明確化すべきと判断)
- 13章の訂正対象に `ecs.tf` / `variables.tf:40` を追加(2章の指摘が訂正リストに反映されていなかった)

**2巡目のレビューで【要確認】としていた2点を、その後コードで検証した(結果、両方とも当初の記載を上方修正)**

- **4-3 Cart-Token**: 【確認済】に格上げ。WooCommerce **6.3.1** の Blocks に存在しないことを確認。
  さらに**既存の提案文書 `headless-migration-proposal.md:86,271` が Cart-Token を前提に
  「ヘッドレスに親和性が高い」と論じている**ことを発見。訂正対象として13章に追加した。
  当初は「BFF設計に影響」程度の扱いだったが、**提案の論拠そのものに関わる**ため記述を拡充
- **12-1 nonceフィルタ**: 【確認済】に格上げ。実在に加え、**WooCommerce本体が `Checkout.php:340`
  で自ら使っている**ことを発見。「公式に前例がある」ためレビューで止めにくいという、
  当初の想定より悪い状況。対策を「明文化」から「**CIでgrep検出**」に具体化した
  (これはG1/G2の適用先としても機能する)

**なお残る不確実性**
- G2の判定基準(8章)は案であり、実施方法は未確定
- 9章のコスト試算は `desired_count=2` を反映したが、WP側の必要スペック(1vCPU/2GB)は
  実測ではなく想定値。プラグイン92個の実負荷は未計測
- 判断2(uploads)の推奨は「JSONレスポンス検証には不要」という論拠だが、
  **環境差分を減らす方針とはトレードオフの関係にある**。判断を委ねている
